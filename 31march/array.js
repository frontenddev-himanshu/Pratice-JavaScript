let a = [1 , 2, 3];
let b = [4, 5, 6];

// console.log(a + b); // 1, 2, 34, 5, 6

console.log([] == []);

// output
// Arrays in JavaScript are like containers.
// When you create two arrays using [], each array is a separate container in memory.
// Even though both arrays are empty, they are two different containers.
// Comparing arrays means checking if they point to the same container. Since these two arrays are different, the comparison gives false.


let arr  = [, , ,];
// console.log(arr.length); 3


// console.log([] + 1); // 1
// console.log([1] + 1); // 11
// console.log([1, 2, 3] + 1); // 1,2,31






